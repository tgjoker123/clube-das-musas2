/**
 * Cliente HTTP tipado, gerado automaticamente a partir do Swagger/OpenAPI
 * de apps/api (ver docs/01_ESTRUTURA_DO_PROJETO.md §7). Nunca editado à
 * mão — regenerado a cada mudança de contrato da API, a partir da Fase 1
 * de implementação, quando o primeiro endpoint existir.
 */
export {};
